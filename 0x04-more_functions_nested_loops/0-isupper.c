#include "main.h"
/**
 * _isupper - check upper cases
 *  @c: value to check
 * Return: 1 if successful else 0 
 */
int _isupper(int c)
{
	char k;
	for (k = 'A'; k <= 'Z' k++)
	{
		if (c != k)
		{
			return(0);
		}
		else
		{
			return(1);
			break:
		}
	}
}	
